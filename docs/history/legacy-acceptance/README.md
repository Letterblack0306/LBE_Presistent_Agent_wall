# Legacy Acceptance Fixtures

This directory contains the legacy CEP/Guard Inspector acceptance fixture and is not the active
LBE implementation gate. The active acceptance namespace is `docs/acceptance/`, whose current
plan is declared by `.lbe/governance/implementation-gates.json`.

Do not use `post_fix_acceptance_plan.json` to infer the current LBE phase, runtime architecture,
or unfinished implementation work.
