# Current Technical Contracts

These documents define current implementation registries. They are neither the current status nor
acceptance proof; use the [documentation library](../README.md) for routing.

- `PRIORITY_MODULE_REGISTRY.md` — module ownership and implementation priority registry.
- `VALIDATED_WORKSPACE_MEMORY.md` — validated workspace-memory contract and links to its module
  registry.
