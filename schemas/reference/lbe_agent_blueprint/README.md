# LBE Blueprint Schemas (Historical Reference)

These JSON schemas originated from the earlier LBE agent blueprint.

- `project_profile.schema.json` — project profile schema
- `tool.schema.json` — tool capability schema

They are **retained for reference only** and do not define the current
runtime contract. The current runtime is deterministic, read-only, and
does not use these schemas for validation.
